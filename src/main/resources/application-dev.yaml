spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/backend_template_security
    username: backend_template_security
    password: backend_template_security
    driver-class-name: org.postgresql.Driver

  jackson:
    serialization:
      write-dates-as-timestamps: false

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  sql:
    init:
      mode: never

  flyway:
    enabled: true
    locations: classpath:db/migration

security:
  jwt:
    secret-key: aTSvmUXBiooktgiD5cdyw3IM0W+8AMfhh5nRiCZmQ9o=